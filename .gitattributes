# Normaliza finales de línea: en el repo siempre LF.
* text=auto eol=lf

# Scripts de shell siempre LF (evita romperlos en Windows).
*.sh text eol=lf

# Binarios (no normalizar).
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
*.woff binary
*.woff2 binary
*.pdf binary
